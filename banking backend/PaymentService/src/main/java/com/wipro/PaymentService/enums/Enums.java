package com.wipro.PaymentService.enums;

public class Enums {
	public enum KycStatus {
		ACTIVE, INACTIVE, PENDING, REJECTED

	}

	public enum AccountType {
		SAVINGS, CURRENT

	}

	public enum Gender {
		MALE, FEMALE, OTHERS
	}

	public enum BankAccountType {
		CURRENT, SAVINGS, SALARY

	}
}
